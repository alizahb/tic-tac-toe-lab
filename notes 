/*id message (#message) where game status prints 
class board (.board)
    div class sqr (for each space on the board)
    id 0 -8 (9 total squares)




 
The addEventListener method will provide the event object for the handle click function:
    const handleClick = (event) => { console.log(event) placePiece(event.target.id); checkForWinner(); checkForTie(); switchPlayerTurn(); render(); } 
    -remove valueOf() 

*/